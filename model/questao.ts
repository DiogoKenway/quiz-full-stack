export default class QuestaoModel {
    #id: number;
    #enuncido: string;
    #resposta: any[];
    #acertou: boolean;
    // #respondida: boolean;

    constructor(id: number, enunciado: string, resposta: ) {

    }
}